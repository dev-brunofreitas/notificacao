spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            connectiontimeout: 5000
            timeout: 5000

  thymeleaf:
    enabled: true

envio:
  email:
    remetente:
    nomeRemetente: 'Javanauta'

server:
  port: 8082